/**
 * After the first Cloudflare Pages deploy, set this to your real URL
 * (https://jigsaw.pages.dev or your custom domain). It feeds canonical URLs,
 * Open Graph tags, the sitemap, and robots.txt.
 *
 * Do not use a *.workers.dev URL here — those always include the account subdomain.
 */
export const SITE_URL = 'https://jigsaw.pages.dev';
export const SITE_NAME = 'Jigsaw';

/** Inbox for the home-page feedback form. Leave blank to open the visitor's mail app instead. */
export const FEEDBACK_EMAIL = '';
